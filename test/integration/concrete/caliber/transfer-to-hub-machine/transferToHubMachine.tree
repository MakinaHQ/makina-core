transferToHubMachine.t.sol
├── given recovery mode is off
│  ├── when sender is not the Mechanic
│  │  └── it should revert with UnauthorizedOperator
│  └── when sender is the Mechanic
│     ├── given caliber's token balance is insufficient
│     │  └── it should revert with ERC20InsufficientBalance
│     └── given caliber's token balance is sufficient
│        ├── given token is not priceable
│        │  └── it should revert with FeedDataNotRegistered
│        └── given token is priceable
│           ├── it should decrease the caliber's token balance by amount
│           ├── it should increase the machine's token balance by amount
│           └── it should emit a TransferToHubMachine event
└── given recovery mode is on
   ├── when sender is not the Security Council
   │  └── it should revert with UnauthorizedOperator
   └── when sender is the Security Council
      ├── given caliber's token balance is insufficient
      │  └── it should revert with ERC20InsufficientBalance
      └── given caliber's token balance is sufficient
         ├── given token is not priceable
         │  └── it should revert with FeedDataNotRegistered
         └── given token is priceable
            ├── it should decrease the caliber's token balance by amount
            ├── it should increase the machine's token balance by amount
            └── it should emit a TransferToHubMachine event