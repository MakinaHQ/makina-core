createMachineFromPreDeposit.t.sol
├── when caller does not have the relevant role
│  └── it should revert with AccessManagedUnauthorized
└── when caller has the relevant role
   ├── it should deploy a machine instance, along with a caliber instance
   ├── it should mark the pre-deposit vault's deposit token as an idle token in the machine
   ├── it should transfer the pre-deposit vault's asset balance to the machine
   ├── it should set the machine as owner of the pre-deposit vault's share token
   ├── it should emit a CaliberCreated event
   ├── it should emit a MigrateToMachine event
   └── it should emit a MachineCreated event
