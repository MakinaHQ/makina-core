getSpokeCaliberData.t.sol
├── when the provided chain is not that of a supported spoke chain
│  └── it should return an empty SpokeCaliberData object
└── when the provided chain is that of a supported spoke chain
   └── it should return a SpokeCaliberData object corresponding to spoke caliber deployed on the spoke chain
