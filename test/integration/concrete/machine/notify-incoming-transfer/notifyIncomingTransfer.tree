notifyIncomingTransfer.t.sol
├── when sender is not a machine mailbox
│  └── it should revert with NotMailbox
└── when sender is a machine mailbox
   ├── given the machine has a null token balance
   │  └── it should not change any state
   └── given the machine has a positive token balance
      ├── given the token is already registered as idle in the machine
      │  └── it should not change any state
      └── given the token is not already registered as idle in the machine
         ├── given the token is not priceable against the accounting token via the oracle registry
         │  └── it should revert with FeedDataNotRegistered
         └── given the token is priceable against the accounting token via the oracle registry
            └── it should mark the token as an idle token in the machine
